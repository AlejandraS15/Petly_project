server {
listen 80; 
server_name localhost;
root /usr/share/nginx/html;
index index.html;
# SPA fallback: serve index.html for
location / {

try_files $uri $uri/ /1ndex.html;
}

# Cache static assets
Location /assets/ {
expires 1y;
add _header Cache-Control "public, immutable";
}
}